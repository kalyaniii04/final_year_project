export default function Student() {
  return (
    <div style={{ padding: 20 }}>
      <h2>Student Dashboard</h2>
      <p>Here student will connect wallet and view all certificates issued to them.</p>
      {/* In Phase 2 we will fetch certificates from contract */}
    </div>
  );
}
